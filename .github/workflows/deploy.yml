name: Build & Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install clean-css-cli 
        run: npm install -g clean-css-cli

<<<<<<< HEAD
      - name: Install build tools
        run: npm install --save-dev clean-css-cli terser purgecss

=======
      - name: Minify CSS 
        run: clean-css-cli assets/css/main.css -o assets/css/main.min.css
        
>>>>>>> f3125188e3584098d4515b54e591d03f03db96f5
      - name: Minify CSS
        run: ./node_modules/.bin/cleancss assets/css/main.css -o assets/css/main.min.css

      - name: Minify JS
        run: ./node_modules/.bin/terser assets/js/util.js assets/js/main.js -o assets/js/main.min.js

      - name: Purge unused FontAwesome CSS
        run: |
          ./node_modules/.bin/purgecss \
            --css assets/css/fontawesome-all.min.css \
            --content index.html \
            --output assets/css/

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: cloudflare-test-page
          directory: .
